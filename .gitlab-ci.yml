Environment setup:
  stage: .pre
  script:
    - apt-get -yqq update
    - apt-get -yqq install python3 python3-pip
    - pip3 --no-cache-dir install -r requirements.txt

Testing:
  stage: test
  script:
    - python3 -m pytest
